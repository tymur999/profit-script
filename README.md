### Script for calculating a wallet's PNL for a specified token

Works for any token not just pump.fun

Edit `wallet` and `myToken` public keys as desired

Uses Helius RPC and API for parsing transactions.

FaF Boi
